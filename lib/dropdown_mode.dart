/// [DropdownMode] defines the mode that is used for the dropdown
///
/// [single] is for a single selection child and renders MagicDropdownSingleSelect
/// [multi] is for a multi selection child and renders MAgicDropdownMultiSelect
/// [customChild] will render your custom provided child
enum DropdownMode {
  single,
  multi,
  customChild,
}
