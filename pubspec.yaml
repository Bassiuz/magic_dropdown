name: magic_dropdown
description: A flutter dropdown widget with highly customizable options. Render a custom child in your dropdown menu with custom buttons and sizing!

version: 1.1.3+5
homepage: https://github.com/Bassiuz/magic_dropdown/

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  transparent_pointer: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true